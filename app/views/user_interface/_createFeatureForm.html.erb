<%= form_for @feature, :remote => false, :url => {:controller => 'feature', :action => 'create' } do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.submit "Add Feature", class: "btn btn-small btn-primary" %>
<% end %>